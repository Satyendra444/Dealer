{
  "name": "redis-cache-invalidation-tests",
  "version": "1.0.0",
  "description": "Playwright tests for Redis cache invalidation on devtez.91trucks.com",
  "scripts": {
    "test": "playwright test",
    "test:cache": "playwright test tests/cache/",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "test:list": "playwright test --reporter=list"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^24.10.1"
  }
}
